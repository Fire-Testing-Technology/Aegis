@page "/logout"
@attribute [Authorize]
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Cookie sign-out must happen on a full HTTP request before the response starts.
        // Blazor interactive circuits cannot modify auth cookies once rendering has begun.
        Navigation.NavigateTo("/auth/logout", forceLoad: true);
    }
}
